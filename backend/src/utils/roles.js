/**
 * 👤 User Roles Constants
 * Use these constants throughout the app to avoid hardcoded strings.
 */
const ROLES = {
  CANDIDATE: 'candidate',
  RECRUITER: 'recruiter',
  ADMIN: 'admin',
  SUPER_ADMIN: 'super_admin'
};

module.exports = ROLES;
